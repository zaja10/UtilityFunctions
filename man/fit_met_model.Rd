% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver_met.R
\name{fit_met_model}
\alias{fit_met_model}
\title{Fit and Extract Multi-Environment Trial (MET) Models}
\usage{
fit_met_model(
  object,
  k = 1,
  genotype = NULL,
  site = NULL,
  rotation = "pc",
  ...
)
}
\arguments{
\item{object}{A `design_tableau` object (for fitting) OR a fitted `asreml` object (for extraction).}

\item{k}{Integer. Number of factors for the FA model (default 1).}

\item{genotype}{Character. Column name of the Genotype variable (required if fitting).}

\item{site}{Character. Column name of the Site/Environment variable (required if fitting).}

\item{rotation}{Character. Rotation method: "pc" (Principal Component/SVD) or "varimax".}

\item{...}{Additional arguments passed to \code{asreml()}.}
}
\value{
An object of class \code{fa_asreml} containing loadings, scores, and FAST indices.
}
\description{
The core engine of GenomicFlow. It fits a Factor Analytic (FA) model using ASReml-R
based on a `design_tableau` structure, or extracts parameters from an already fitted model.

It performs post-analysis rotation (SVD/PC or Varimax) to align loadings for biological
interpretation and selection.
}
