% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_vegetation_indices.R
\name{calculate_vegetation_indices}
\alias{calculate_vegetation_indices}
\title{Calculate Vegetation Spectral Indices}
\usage{
calculate_vegetation_indices(
  spectra_df,
  g = 2.5,
  C1 = 6,
  C2 = 7.5,
  L = 1,
  L_savi = 0.5,
  gamma = 1,
  alpha = 0.1,
  sla = 1,
  slb = 0,
  n = 2,
  lambda_N = 800,
  lambda_R = 670,
  lambda_G = 550,
  k = 0,
  c = 1,
  epsilon = 0.1,
  fdelta = 0
)
}
\arguments{
\item{spectra_df}{A data frame where each row is an observation and
each column is a spectral band. Column names must match the expected
band names (e.g., N, R, G, B, RE1, etc.).
Possible band names are: A, B, G, G1, N, N2, PAR, R, RE1, RE2, RE3, S1, S2}

\item{g}{Parameter for certain indices. Default is 2.5.}

\item{C1}{Parameter for certain indices. Default is 6.0.}

\item{C2}{Parameter for certain indices. Default is 7.5.}

\item{L}{Parameter for certain indices. Default is 1.0.}

\item{L_savi}{Parameter for certain indices. Default is 0.5.}

\item{gamma}{Parameter for certain indices. Default is 1.0.}

\item{alpha}{Parameter for certain indices. Default is 0.1.}

\item{sla}{Parameter for certain indices. Default is 1.0.}

\item{slb}{Parameter for certain indices. Default is 0.0.}

\item{n}{Parameter for certain indices. Default is 2.0.}

\item{lambda_N}{Parameter for certain indices. Default is 800.0.}

\item{lambda_R}{Parameter for certain indices. Default is 670.0.}

\item{lambda_G}{Parameter for certain indices. Default is 550.0.}

\item{k}{Parameter for certain indices. Default is 0.0.}

\item{c}{Parameter for certain indices. Default is 1.0.}

\item{epsilon}{Parameter for certain indices. Default is 0.1.}

\item{fdelta}{Parameter for certain indices. Default is 0.0.}
}
\value{
A data frame with the original spectral bands plus new columns
  for each successfully calculated spectral index.
}
\description{
This function calculates all possible vegetation indices from the 'Awesome
Spectral Indices' list based on the columns present in the input data frame.
It requires the 'dplyr' and 'rlang' packages.
}
\examples{
\dontrun{
# Install packages if you don't have them
# install.packages(c("dplyr", "rlang"))
library(dplyr)

# Create dummy data with ONLY N, R, G, B bands
# This matches your common use case
spectra_data_simple <- data.frame(
  N = c(0.8, 0.75),
  R = c(0.1, 0.12),
  G = c(0.2, 0.22),
  B = c(0.1, 0.11)
)

# This will only calculate indices that use N, R, G, and/or B
# (e.g., NDVI, GNDVI, GRVI, etc.)
# It will print a message: "Calculating 23 indices: ..."
simple_indices <- calculate_vegetation_indices(spectra_data_simple)
print(names(simple_indices))

# Create more complex data with Red Edge and SWIR bands
spectra_data_complex <- data.frame(
  N = c(0.8, 0.75), R = c(0.1, 0.12), G = c(0.2, 0.22), B = c(0.1, 0.11),
  RE1 = c(0.3, 0.33), RE2 = c(0.4, 0.44), RE3 = c(0.5, 0.55),
  S1 = c(1.6, 1.65), S2 = c(2.1, 2.15), A = c(0.05, 0.06),
  PAR = c(2000, 2100), G1 = c(0.21, 0.23), N2 = c(0.81, 0.76)
)

# This will calculate all 129 indices
# It will print a message: "Calculating 129 indices: ..."
complex_indices <- calculate_vegetation_indices(spectra_data_complex)
print(names(complex_indices))
}
}
